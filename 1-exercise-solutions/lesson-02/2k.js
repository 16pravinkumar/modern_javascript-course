Math.ceil(2.2)


// what does this Math.ceil(2.2)
// represent in js

// In JavaScript, `Math.ceil()` is a method that rounds a number up to the nearest integer greater than or equal to the given number. 
// So, `Math.ceil(2.2)` would represent rounding up 2.2 to the nearest integer, which is 3.